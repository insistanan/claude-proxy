# 手动分析方法

运行完成后，页面会显示本 Mod 的源目录、冻结快照目录、本次分析目录和已经替换好路径的示例命令。

将你自己的 Java、EXE 或其他程序放在任意位置，读取 `analysis-input.json`，并按 `result-schema.json` 生成 `analysis-output.json`。随后可在页面粘贴或导入 JSON 结果。外部程序由用户手动运行，项目不会自动执行。
