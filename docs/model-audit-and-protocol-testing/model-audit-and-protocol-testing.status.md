# 渠道模型审计与协议测试执行状态

> 这是 Goal 的唯一可变执行游标，更新时覆盖旧状态，不追加流水账。
> 文件长期保持在 120 行以内。

## 0. 快速恢复头

| 字段 | 当前值 |
| --- | --- |
| Goal ID | MAPT-2026-08-11 |
| 总体状态 | 已完成；未授权验证已交接用户 |
| 当前主线 | M8：模拟验证和用户验收交接 |
| 当前主线状态 | 已完成 |
| 下一主线 | 无；全局完成条件已复核 |
| 最近完成主线 | M8：模拟验证和用户验收交接 |
| 阻塞 | 无 |
| 方案导入锁 | 已生成并由用户确认；恢复时禁止全文重读 |

## 1. 每次恢复只做这些

1. 阅读适用的 `AGENTS.md`。
2. 完整阅读本 Status。
3. 检查工作区状态和最近变更，识别用户改动归属。
4. 在 Goal 任务账本中只读取 M8 对应行及明确引用的规则。
5. 定点读取 `plan/00-overview.md` 的完成条件和 `plan/05-verification-rollout.md` 的验证/交接部分。
6. 从“下一步唯一动作”继续。

禁止为了恢复而通读整个 Goal、整套方案、全部历史分析或重新执行全局审计。

## 2. 当前主线：M8

目标：

完成授权内模拟验证，补齐交接前暴露的生产健壮性缺口，并交付真实渠道、完整前端检查与构建验收边界。

本主线不做：

不调用真实渠道，不运行完整前端 type-check/构建/打包，不部署发布，不把未验证行为写成已通过。

完成条件：

- 生产调度和长运行租约不存在已知的空实现或过期并发窗口。
- 获授权的固定夹具覆盖统一执行、身份/混用、能力、调度、展示和管理 API 主路径及失败边界。
- 静态检查确认前端使用稳定 ID、注册图标、冻结 DTO 和默认脱敏边界，且无真实秘密或无许可证资产进入交付内容。
- 未授权的真实渠道、完整前端检查、构建和打包形成可执行用户验收清单，剩余风险明确。

## 3. 当前主线内部子任务

- [x] 补齐生产定时调度循环与 Runner 租约续期/失租中止语义。
- [x] 注册 Sol reasoning、改写/覆盖、概率、模式差分、变形及元数据生产策略，并接通返回模型混用/时序判定。
- [x] 复核五协议快速测试、演武台、身份、混用和能力的固定场景覆盖。
- [x] 复核运行恢复、预算、取消、不重入、分页、脱敏和 HTML 的固定场景覆盖。
- [x] 补齐首个审计 Job 的多渠道创建、身份/能力配置、启停、编辑和手动运行 UI，再完成前端静态检查。
- [x] 形成真实渠道、前端 type-check、构建、打包和回滚的用户验收清单。
- [x] 完成最终差异、改动归属、秘密和许可证静态检查。

## 4. 当前已确认事实

- M4 身份/混用报告、M5 能力报告和 M6 版本化 SQLite 载荷是展示事实源，前端不得重新判分。
- 摘要不覆盖现有健康/延迟字段；身份风险与生产健康使用不同 API 字段、颜色和标签。
- 摘要必须表达未检测、进行中、部分失败、不支持、证据不足和已过期，新鲜度阈值随报告类型或策略配置。
- 详情按摘要、身份与混用、能力、稳定性与性能、运行样本、配置与版本组织，并保留证据引用。
- HTML 单文件内嵌 CSS/数据，不依赖 CDN；默认排除密钥、Authorization、保留题答案和未选择的完整输入。
- 管理 API 覆盖任务 CRUD/启停、手动运行、取消、运行列表/详情、最新摘要、报告详情和 HTML 导出。
- 生产 Runner 已接入身份/能力任务、预算和取消；活动租约按租期三分之一续期，完成前停止续租，CAS 失租会取消在途执行并由持久化恢复合同按 interrupted 收口。
- 生产注册表现有 7 个独立身份策略；Sol 行为探针均为保守非正式证据，缺少授权概率基线时显式证据不足；返回模型序列可生成替换、混用比例和阶段切换，行为信号不能单独误报正式身份。
- 生产调度器按稳定 ID 分页扫描 enabled Job，只触发扫描窗口内最新槽位；启动及每轮协调 pending/running 恢复，关闭顺序先调度、再 HTTP、再活动 Runner。
- 用户验收顺序已冻结为备份、前端检查、版本注入打包、隔离配置五协议/审计/6 小时调度、重启恢复、渠道状态不变核对和旧二进制回滚；失败保留脱敏证据。
- 审计任务 UI 已覆盖首个 Job 创建、多渠道稳定 ID、身份/能力配置、调度/预算、乐观锁 CRUD、手动运行及分页历史；不可解析的历史目标、策略或精确预设版本会阻止编辑，不会静默丢失或升级。

## 5. 当前改动与资料归属

本 Goal 当前改动包括规划工作区、`backend-go/internal/modelaudit/`、`backend-go/main.go` 及快速测试/演武台相关前端文件，均归属本 Goal。未发现 Goal 外未提交改动。

## 6. 最近验证

| 验证项 | 结果 | 证据或时间点 |
| --- | --- | --- |
| 最终归属与发布静态检查 | 通过 | 实现文件真实秘密形态、本地参考路径/TODO/资产扫描零命中，全前端 MDI 零缺失，已跟踪及未跟踪文件 whitespace 检查通过；项目测试/构建未运行 |
| M1 静态/模拟验证 | 通过 | `GOPROXY=off go test ./internal/modelaudit`，0.161s，2026-08-11 |
| M2 离线聚焦验证 | 通过 | 五协议、Responses 终态、多轮、取消、工具调用和管理 API；`GOPROXY=off go test ./internal/modelaudit`，0.154s，2026-08-11 |
| M3 离线聚焦验证 | 通过 | 策略注册/配置哈希、组合拒绝、基线授权/哈希、报告冻结；`GOPROXY=off go test ./internal/modelaudit`，0.167s，2026-08-11 |
| M4 身份固定夹具 | 通过 | 匹配、替换、混用比例与区间、OOD、阶段切换、短期异常、相关组限权、基线兼容和样本不足；`GOPROXY=off go test ./internal/modelaudit`，0.175s，2026-08-11 |
| M5 能力合同固定夹具 | 通过 | 七维任务包、规范化快照哈希、任务实例、单题得分/不支持状态和冻结答案一致性；`GOPROXY=off go test ./internal/modelaudit`，0.147s，2026-08-11 |
| M5 判分、聚合与区间夹具 | 通过 | 客观判分、七维聚合、覆盖率、固定种子置信区间及证据不足；`GOPROXY=off go test ./internal/modelaudit`，2026-08-11 |
| M5 预设、预算与公平比较夹具 | 通过 | quick/standard/deep、正式资格、最坏预算、协议/思考/模型公平快照及稳定原因码；`GOPROXY=off go test ./internal/modelaudit`，2026-08-11 |
| M5 端到端场景夹具 | 通过 | 优秀、部分正确、随机错误、格式错误、协议不支持和样本不足经判分/结果/七维聚合得到预期；`GOPROXY=off go test ./internal/modelaudit`，0.175s，2026-08-11 |
| M6 调度/运行合同夹具 | 通过 | 多目标、工作负载、6 小时至 30 天间隔、结束/持续时间、预算、运行终态、默认模型解析、租约及载荷哈希；`GOPROXY=off go test ./internal/modelaudit`，0.168s，2026-08-11 |
| M6 任务生命周期夹具 | 通过 | 稳定 ID、创建/更新、乐观版本、目标身份、状态机、软删除、已结束调度及 409 冲突码；`GOPROXY=off go test ./internal/modelaudit`，0.168s，2026-08-11 |
| M6 调度计算与生产循环夹具 | 通过 | 最新到期槽位、稳定抖动/边界、短 tick 启停、重启不补跑、定时原子启动及关闭停止触发；`GOPROXY=off go test ./internal/modelaudit`，1.297s，2026-08-11 |
| M6 运行协调与生产租约夹具 | 通过 | 手动/定时真实槽位、全局并发、长请求跨租期续租、完成清理、CAS 失租取消及 interrupted 恢复；`GOPROXY=off go test ./internal/modelaudit`，1.144s，2026-08-11 |
| M6 预算与取消夹具 | 通过 | 目标解析、四类预算预检、usage 累加、预算部分结果、启动前/启动后取消及失败终态；`GOPROXY=off go test ./internal/modelaudit`，0.158s，2026-08-11 |
| M6 SQLite 与恢复夹具 | 通过 | 独立 Schema、CAS、稳定 Job 扫描、启动/周期恢复、pending 换租约执行及 running 中断收口；`GOPROXY=off go test ./internal/modelaudit`，1.297s，2026-08-11 |
| M6 综合固定场景 | 通过 | 两渠道、错过周期跳过、数据库不重入、预算部分停止、手动取消不改计划、pending 恢复及 running 中断收口；`GOPROXY=off go test ./internal/modelaudit`，0.363s，2026-08-11 |
| M7 展示合同夹具 | 通过 | 摘要/详情/导出 DTO、新鲜度阈值、主状态优先级、运行安全视图及不可放宽的默认脱敏清单；`GOPROXY=off go test ./internal/modelaudit`，0.382s，2026-08-11 |
| M7 管理 API 与生产运行夹具 | 通过 | 全路由族、安全 DTO、资产哈希/种子/脱敏、身份及能力 quick/standard/预算部分执行、覆盖率浮点边界；`GOPROXY=off go test ./internal/modelaudit`，0.866s，2026-08-11 |
| M7/M8 前端静态检查 | 通过 | 稳定 ID 路由、Job/Run 分页、精确版本保留、409 恢复、脱敏、响应式、44px 操作、ARIA 及 MDI 注册零缺失；`git diff --check`，2026-08-11 |
| M7 最终合同夹具 | 通过 | 八种摘要状态、前端嵌套 JSON、分页元数据、默认脱敏及 HTML 自包含；`GOPROXY=off go test ./internal/modelaudit`，0.870s，2026-08-11 |
| M8 生产身份策略夹具 | 通过 | 7 策略注册、Sol 规划/评估、无基线证据不足、返回模型 50/50 混用及阶段切换；`GOPROXY=off go test ./internal/modelaudit`，1.417s，2026-08-11 |
| 前端完整 type-check/构建 | 未运行 | 遵守用户验证边界 |

## 7. 阻塞问题

无。

## 8. 下一步唯一动作

用户按交接清单执行真实渠道、前端 type-check、构建、版本注入打包和回滚验收；若反馈本 Goal 范围缺陷，再按恢复协议重新激活。

## 9. 下一主线

无。M1-M8 和全局完成条件均已有授权内证据；真实渠道和完整构建仍不得标记为已验证。

## 10. Status 更新规则

- 始终只保留一个当前主线、一个下一步唯一动作和一个下一主线。
- 当前主线内部最多保留 7 个子任务。
- 只在任务开始、原子进展完成、中断、真实阻塞、验证变化或切换主线时覆盖更新。
- 删除失去恢复价值的细节，不记录普通读取、重复搜索、失败尝试和旁支流水。
- 超过 120 行时先压缩旧内容。
